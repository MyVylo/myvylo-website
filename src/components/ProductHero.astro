<section class="product-hero page-width" id="product-preview" data-product-story aria-label="See how Vylo organizes your money">
  <p class="sr-only" data-story-description>Fictional transactions arrive in Vylo, are categorized into budgets, and update the linked account balance. Net worth reflects the account balance changes.</p>
  <iframe src="/product/hero/us.html" title="Animated demonstration using Vylo’s actual product components" data-story-frame tabindex="-1" aria-hidden="true" inert loading="eager"></iframe>
  <div class="story-toolbar"><div class="story-beats" aria-label="Animation chapters"><button type="button" data-story-beat="0" aria-label="Show transactions animation">Transactions</button><i>→</i><button type="button" data-story-beat="1" aria-label="Show budgets animation">Budgets</button><i>→</i><button type="button" data-story-beat="2" aria-label="Show net worth animation">Net worth</button></div></div>
</section>
<style>
  .product-hero { position:relative; isolation:isolate; container-type:inline-size; }
  .product-hero::before { content:''; position:absolute; inset:2% 0 5%; z-index:-1; background:radial-gradient(ellipse at 50% 55%,#d5c9e780,transparent 70%); pointer-events:none; }
  .product-hero iframe { display:block; width:100%; height:calc((100cqw - 96px) * .654545 + 60px); border:0; color-scheme:normal; }
  .sr-only { position:absolute; width:1px; height:1px; overflow:hidden; clip-path:inset(50%); }
  .story-toolbar,.story-beats { display:flex; align-items:center; justify-content:center; }
  .story-toolbar { gap:35px; }.story-beats { gap:13px; font-size:12px; color:#91899b; }.story-beats i { font-style:normal; color:#c0b7c9; }.story-beats button[data-active="true"] { color:#5744cb; }
  .story-beats button { border:0; background:none; color:inherit; font:inherit; padding:5px 0; cursor:pointer; }.story-beats button:hover { color:#5744cb; }.story-beats button:focus-visible { outline:2px solid #5744cb; outline-offset:4px; border-radius:3px; }
  @container(max-width:900px) { .product-hero iframe { height:730px; } }
  @media(max-width:700px) { .product-hero.page-width { width:100%!important; }.story-toolbar { flex-direction:column; gap:12px; }.story-beats { font-size:11px; gap:9px; } }
</style>
<script>import '../scripts/product-hero';</script>
